These SQL files are preserved as local historical artifacts that do not belong to the canonical remote migration chain.

The production-aligned history used by Supabase CLI now lives in `supabase/migrations/` and matches the versions stored in `supabase_migrations.schema_migrations`.
